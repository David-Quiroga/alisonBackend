//! NO VALE 🚩
/* const express = require('express');
const router = express.Router();
const csrf = require('csurf');
const csrfProtection = csrf({ cookie: true }); // Configuración específica

const serviceCategoryController = require('../controller/serviceCategoryController');

router.post('/service-category', csrfProtection, serviceCategoryController.create);
router.get('/service-category', serviceCategoryController.getAll);
router.put('/service-category/:id', csrfProtection, serviceCategoryController.update);
router.delete('/service-category/:id', csrfProtection, serviceCategoryController.delete);

module.exports = router; */